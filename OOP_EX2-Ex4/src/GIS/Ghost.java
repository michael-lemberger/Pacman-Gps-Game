package GIS;

import java.awt.Image;
import Geom.Point3D;

public class Ghost extends Pacman {
	public Ghost(Point3D p, int id, double speed, double radius, Image img) {
		super(p, id, speed, radius, img);
	}
}